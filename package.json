{
    "name": "my_workspace",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "@bazel/bazelisk": "latest",
        "@bazel/ibazel": "latest",
        "@bazel/buildifier": "latest"
    },
    "scripts": {
        "build": "bazel build //...",
        "test": "bazel test //..."
    },
    "dependencies": {
        "jest": "27.0.3",
        "jest-cli": "^27.0.3"
    },
    "jest": {
        "testEnvironment": "node",
        "transform": {}
    }
}
